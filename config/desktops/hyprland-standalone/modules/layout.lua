-- Scrolling layout configuration

hl.config({
    scrolling = {
        column_width = 0.5,
        follow_focus = true,
        explicit_column_widths = "0.2, 0.5, 0.6, 0.8",
        direction = "right",
        follow_min_visible = 0.5,
        wrap_focus = false,
        wrap_swapcol = false,

    },
})

return true
